import { SliceByTwoPipe } from './slice-by-two.pipe';

describe('SliceByTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceByTwoPipe();
    expect(pipe).toBeTruthy();
  });
});
